"""
The setup script for this package. Requried for installing the package.
"""
from distutils.core import setup
setup(name='excitondiffusion',
        version='1.0',
        package_dir = {'': 'lib'}
        )
